package org.example;

public class NewPaymentProcessor {
    public void processNewPayment(double amount) {
        System.out.println("Processando pagamento pela nova biblioteca: $" + amount);
    }
}
